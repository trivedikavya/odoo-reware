# Secret used to sign JWTs (auth + admin sessions). Use a long random string in production.
JWT_SECRET=replace-this-with-a-long-random-secret-string

# Admin credentials (seeded into data/users.json by `npm run seed`).
# Admin CANNOT be created through the public signup form.
ADMIN_EMAIL=admin@rewear.com
ADMIN_PASSWORD=Admin@12345
